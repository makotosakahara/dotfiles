if [ -z $TMUX ]; then
  export EDITOR=nvim
  # pyenv, pyenv-virtualenv
  export PATH="/home/makoto/.pyenv/bin:$PATH"
  export VIRTUAL_ENV_DISABLE_PROMPT=1
  eval "$(pyenv init -)"
  eval "$(pyenv virtualenv-init -)"
fi
